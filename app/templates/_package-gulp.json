{
  "name": "<%= slug %>",
  "version": "0.0.0",
  "dependencies": {},
  "devDependencies": {
    "gulp": "^3.6",
    "gulp-css-target": "^0.1",
    "compass-options": "^0.1"<% if (server) { %>,
    "browser-sync": "^1.4",
    "gulp-shell": "^0.2"<% } %>
  }
}
